# 🔥 SNAKKONG — Лендинг с WhatsApp интеграцией

Современный лендинг для продажи куриных джерки с автоматическими уведомлениями в WhatsApp.

![SNAKKONG](https://img.shields.io/badge/Made%20with-Node.js-green)
![WhatsApp](https://img.shields.io/badge/WhatsApp-Bot-25D366)
![SEO](https://img.shields.io/badge/SEO-Optimized-blue)

---

## 🚀 Возможности

### Лендинг:
- ✅ Современный тёмный дизайн с огненными эффектами
- ✅ Адаптивная вёрстка (мобильные, планшеты, ПК)
- ✅ Анимации при скролле
- ✅ Форма заказа с валидацией
- ✅ Счётчик количества товаров

### WhatsApp Бот:
- ✅ Автоматические уведомления о заказах
- ✅ Бесплатная отправка сообщений
- ✅ Форматированные сообщения
- ✅ Надёжная работа 24/7

### SEO:
- ✅ Meta теги (Title, Description, Keywords)
- ✅ Open Graph (Facebook, VK, Twitter)
- ✅ Structured Data (JSON-LD)
- ✅ Sitemap.xml
- ✅ Robots.txt
- ✅ PWA поддержка
- ✅ Производительность оптимизирована

---

## 📦 Установка

### 1. Клонируйте репозиторий:
```bash
cd C:\Users\Sanjik\Desktop\Vlad-Landing-KKong
```

### 2. Установите зависимости:
```bash
npm install
```

### 3. Настройте номер телефона:
Откройте `server.js` и измените на строке 13:
```javascript
const ADMIN_PHONE = '77085888664'; // ← Ваш номер БЕЗ +
```

### 4. Запустите сервер:
```bash
npm start
```

### 5. Отсканируйте QR-код:
- Откройте WhatsApp на телефоне
- Меню → **Связанные устройства** → **Привязать устройство**
- Наведите камеру на QR-код в терминале

### 6. Откройте сайт:
```
http://localhost:3000
```

---

## 🛠 Технологии

| Технология | Описание |
|------------|----------|
| **Node.js** | Серверная часть |
| **Express** | Web фреймворк |
| **whatsapp-web.js** | WhatsApp интеграция |
| **HTML5 / CSS3** | Разметка и стили |
| **JavaScript** | Интерактивность |

---

## 📁 Структура проекта

```
Vlad-Landing-KKong/
├── index.html              # Главная страница
├── styles.css              # Стили
├── script.js               # Клиентский JavaScript
├── server.js               # Node.js сервер
├── package.json            # Зависимости
├── sitemap.xml             # Карта сайта для SEO
├── robots.txt              # Правила для поисковиков
├── manifest.json           # PWA манифест
├── .htaccess               # Настройки Apache
├── SEO-CHECKLIST.md        # Чек-лист по SEO
└── README.md               # Документация
```

---

## 🌐 Деплой на хостинг

### Вариант 1: VPS/VDS (рекомендуется)
```bash
# 1. Подключитесь к серверу
ssh user@your-server.com

# 2. Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 3. Клонируйте проект
git clone <your-repo-url>
cd Vlad-Landing-KKong

# 4. Установите зависимости
npm install

# 5. Настройте PM2 (автозапуск)
sudo npm install -g pm2
pm2 start server.js --name snakkong
pm2 save
pm2 startup

# 6. Настройте Nginx (порт 80/443)
sudo nano /etc/nginx/sites-available/snakkong
```

**Конфиг Nginx:**
```nginx
server {
    listen 80;
    server_name snakkong.ru www.snakkong.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Вариант 2: Heroku
```bash
# 1. Установите Heroku CLI
# 2. Создайте Procfile:
echo "web: node server.js" > Procfile

# 3. Деплой
heroku create snakkong
git push heroku main
```

### Вариант 3: Vercel / Netlify
❌ Не подходит — нужен постоянный WebSocket для WhatsApp

---

## 📱 Настройка SEO после деплоя

1. **Измените URL в файлах:**
   - `index.html` — все `https://snakkong.ru`
   - `sitemap.xml` — все URL
   - `robots.txt` — Sitemap URL

2. **Создайте изображения:**
   - `favicon.ico` (16x16, 32x32)
   - `og-image.jpg` (1200x630)
   - `icon-192.png`, `icon-512.png`

3. **Зарегистрируйтесь:**
   - Google Search Console
   - Yandex Webmaster
   - Google Analytics
   - Yandex.Metrika

4. **Подробнее:** см. `SEO-CHECKLIST.md`

---

## 🔧 Настройки

### Изменить порт сервера:
`server.js` строка 10:
```javascript
const PORT = process.env.PORT || 3000;
```

### Изменить часовой пояс:
`server.js` строка 91:
```javascript
timeZone: 'Asia/Almaty',
```

### Изменить email:
`index.html` строка около 250:
```html
<a href="mailto:info@snakkong.ru" class="contact-email">
```

---

## 📊 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Главная страница |
| `/api/status` | GET | Статус WhatsApp |
| `/api/order` | POST | Создать заказ |
| `/api/test` | POST | Тестовое сообщение |

---

## ⚠️ Важные замечания

1. **WhatsApp сессия:**
   - QR-код нужно сканировать только 1 раз
   - Сессия сохраняется в `.wwebjs_auth/`
   - При переносе на другой сервер — нужно заново

2. **Безопасность:**
   - Не публикуйте `.wwebjs_auth/` в Git
   - Используйте HTTPS на проде
   - Добавьте rate limiting для API

3. **Ограничения WhatsApp:**
   - Не рассылайте спам (забанят)
   - Максимум ~100 сообщений/час
   - Только сохранённые контакты

---

## 🐛 Troubleshooting

### Ошибка: "EADDRINUSE port 3000"
```bash
# Windows:
netstat -ano | findstr :3000
taskkill /F /PID <PID>

# Linux:
lsof -ti:3000 | xargs kill -9
```

### WhatsApp не подключается:
1. Проверьте интернет
2. Перезапустите сервер
3. Удалите `.wwebjs_auth/` и отсканируйте заново
4. Обновите Chrome: `npx @puppeteer/browsers install chrome@stable`

### Сообщения не отправляются:
1. Проверьте номер в `ADMIN_PHONE`
2. Проверьте, что WhatsApp подключен (зелёная галочка)
3. Проверьте логи: `pm2 logs snakkong`

---

## 📞 Контакты

**Email:** info@snakkong.ru  
**WhatsApp:** +7 708 588-86-64  
**Telegram:** @snakkong

---

## 📄 Лицензия

Made with ❤️ by BSanjik  
© 2026 SNAKKONG

---

## 🎯 Roadmap

- [ ] Добавить корзину товаров
- [ ] Интеграция с оплатой (Kaspi, Halyk)
- [ ] Личный кабинет клиента
- [ ] Программа лояльности
- [ ] Telegram бот
- [ ] Мобильное приложение

**Удачи! 🚀**
